import EStyleSheet from 'react-native-extended-stylesheet';

export const VenueScreenStyles = EStyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 6
  },
  headerTextStyle: {
    fontFamily: '$light',
    fontSize: 15
  }
});
