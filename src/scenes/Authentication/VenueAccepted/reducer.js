import {  } from './actions';

const VenueAcceptedReducer = (state = { fetchingVenueAcceptedUtils: false }, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default VenueAcceptedReducer;
