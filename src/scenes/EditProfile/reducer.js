import {  } from './actions';

const EditProfileReducer = (state = { fetchingEditProfileUtils: false }, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default EditProfileReducer;
